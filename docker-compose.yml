services:
  frontend:
    build: ./frontend
    ports:
      - "5174:5174"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file: ./backend/.env
    depends_on:
      - db
      - minio
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  db:
    image: postgres:18
    container_name: footbolski-db-local
    environment:
      POSTGRES_USER: footbolski
      POSTGRES_PASSWORD: footbolski
      POSTGRES_DB: footbolski
    volumes:
      - pgdata:/var/lib/postgresql
    ports:
      - "5435:5432"

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: KndaqJQbF0crMDWf8HyZUVizx1wXpjBv
    volumes:
      - miniodata:/data
    ports:
      - "9000:9000"
      - "9001:9001"

volumes:
  pgdata:
  miniodata:
